import styled from "@emotion/styled";

export const Header = styled("div")({
  padding: "24px",
  background: "black",
});
export const ContentDispaly = styled("div")({
  display:"flex",
  flexWrap:"wrap",
  alignItems:"center",
  justifyContent:"flex-end",
});
